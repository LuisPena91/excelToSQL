python -m pip install --<<upgrade>> pip --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host files.pythonhosted.org

myenv\Scripts\activate

https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_execution_policies?view=powershell-7.4

#Conectar a la BD
conexion = mysql.connector.connect(
    host='localhost',
    user='root',
    password='Lfpp0811.*',
    database='productivity_app'
)
cursor = conexion.cursor()




# Cerrar la conexión
cursor.close()
conexion.close()


#conectar a la BD
dialecto = 'mysql'
usuario = 'root'
contraseña = 'Lfpp0811.*'
host = 'localhost'  # O la dirección IP de tu servidor
nombre_bd = 'productivity_app'

# Crear la cadena de conexión
engine = create_engine(f'{dialecto}://{usuario}:{contraseña}@{host}/{nombre_bd}')

IDEA: CREAR UN DICCIONARIO PARA ALMACENAR "KEY" = COLUMNA DE LA DB. "VALUE" = COLUMNA DE EXCEL


Para el jueves 05:
crear un ciclo que recorra cada una de las columnas de la tabla de SQL
y pregunte que columna de la hoja de excel le desea asignar (o asociar)




                if result:
                    for i in range(len(dic_keys)-1):
                        cursor.execute(
                            f"UPDATE {table_selected} SET {dic_keys[i+1]} = {row[dic_values[i+1]]} WHERE {dic_keys[0]} = {row[dic_values[0]]}"
                        )
                        print(f"updated value = {dic_keys[i+1]} -  whit the value = {row[dic_update[dic_keys[i+1]]]}")